<h1 align="center">Welcome to <%= @user.name %>'s page</h1>
<div class="row">
	<div class="col-md-4 offset-4 center">
		<%= image_tag(@user.profile_image.url, size: "200x200", class: "rounded-circle") if @user.profile_image? %>
	</div>
</div>

<h4 align="center"><%= @user.name %>'s articles</h4>

<div class="d-flex justify-content-center">
	<%= will_paginate @user_articles %>
</div>

<%= render 'articles/article', obj: @user_articles %>

<div class="d-flex justify-content-center">
	<%= will_paginate @user_articles %>
</div>